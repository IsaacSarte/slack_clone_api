import React from 'react'

const SearchBar = (props) => {

    // Props
    const {
        userDB,
        setUserDB,
        setChatWith,

    } = props;

    /* State Management */

    return (
        <div>
            Search Bar
        </div>
    )
}

export default SearchBar;
